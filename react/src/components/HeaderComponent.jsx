import React from 'react'
import TitleComponent from './TitleComponent'

export default function HeaderComponent({ title, buttons = "", children }) {

    return (
            <TitleComponent title="Campaigns">
                </TitleComponent>
    )
}
